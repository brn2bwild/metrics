<x-app-layout>
    {{-- <x-slot name="header">
        <h2 class="font-semibold text-xl text-gray-800 leading-tight">
            {{ __('Dashboard') }}
        </h2>
    </x-slot> --}}
    <div class="bg-gray-600" style="background-image: url('{{asset('storage/images/jumbotron-image.jpg')}}'); background-size: cover;background-position: center;">
      <div class="max-w-7xl mx-auto sm:px-6 lg:px-8 flex justify-center">
        <div class="grid grid-cols-4 md:grid-cols-8">
          <div class="bg-red-500 col-span-4 text-center md:text-right p-8">
            <h1 class="">Responsive left-aligned hero with image andlaondñakndlñ in ñasoind aslñkdnas ldknas dñlkasnd ñalksnd akldnañ sdlkasd nñaslkd masd</h1>
          </div>
          <div class="bg-red-500 col-span-4 text-center">
            <h1 class="display-5 fw-bold lh-1 mb-3">Responsive left-aligned hero with image</h1>
          </div>
        </div>
      </div>
    </div>
</x-app-layout>
          {{-- @if (Route::has('login'))
              <div class="hidden fixed top-0 right-0 px-6 py-4 sm:block">
                  @auth
                      <a href="{{ url('/dashboard') }}" class="text-sm text-gray-700 dark:text-gray-500 underline">Dashboard</a>
                  @else
                      <a href="{{ route('login') }}" class="text-sm text-gray-700 dark:text-gray-500 underline">Log in</a>

                      @if (Route::has('register'))
                          <a href="{{ route('register') }}" class="ml-4 text-sm text-gray-700 dark:text-gray-500 underline">Register</a>
                      @endif
                  @endauth
              </div>
          @endif --}}
          {{-- <div class="p-5 mb-4 bg-black jumbotron-imagen">
            <div class="row flex-lg-row-reverse items-center g-5 py-5">
                <div class="col-lg-6">
                    <h1 class="display-5 fw-bold lh-1 mb-3">Responsive left-aligned hero with image</h1>
                    <p class="lead">Quickly design and customize responsive mobile-first sites with Bootstrap, the world’s most popular front-end open source toolkit, featuring Sass variables and mixins, responsive grid system, extensive prebuilt components, and powerful JavaScript plugins.</p>
                    <div class="d-grid gap-2 d-md-flex justify-content-md-start">
                    <button type="button" class="btn btn-primary btn-lg px-4 me-md-2">Primary</button>
                    <button type="button" class="btn btn-outline-secondary btn-lg px-4">Default</button>
                    </div>
                </div>
            </div>
          </div> --}}

            
